services:
  app-025:
    build: .
    container_name: app-025-codegen-03
    ports:
      - "8125:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/healthz"]
      interval: 15s
      timeout: 3s
      retries: 3
      start_period: 5s
